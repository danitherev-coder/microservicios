spring:
  application:
    name: gateway-service 
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

logging:
  level:
    root: INFO
    org:
      springframework:
        cloud:
          gateway: TRACE
          gateway.filter: TRACE
          gateway.handler.predicate: TRACE
          gateway.route.RouteDefinitionRouteLocator: INFO

